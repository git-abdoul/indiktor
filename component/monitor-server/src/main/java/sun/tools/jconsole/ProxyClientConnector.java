package sun.tools.jconsole;


public class ProxyClientConnector {

//	public static void connect(ProxyClient client) {
//		if (client != null && !isConnected(client))
//			client.connect();
//	}
//	
//	public static void flush(ProxyClient client) {
//		if (client != null && isConnected(client))
//			client.flush();
//	}
//	
//	public static boolean isConnected(ProxyClient client) {
//		if (client != null && client.isConnected() && client.getConnectionState() == ConnectionState.CONNECTED)
//			return true;
//		else
//			return false;
//	}
}
